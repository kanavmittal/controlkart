import Link from "next/link"

const COLUMNS = [
  {
    title: "Products",
    links: [
      { href: "/categories/plcs", label: "PLCs" },
      { href: "/categories/plc-accessories", label: "PLC Accessories" },
      { href: "/categories/hmis", label: "HMIs" },
      { href: "/categories/timers-counters", label: "Timers & Counters" },
      { href: "/categories/energy-meters", label: "Energy Meters" },
    ],
  },
  {
    title: "Procurement",
    links: [
      { href: "/quick-order", label: "Quick Order by SKU" },
      { href: "/request-quote", label: "Request a Quote" },
      { href: "/account", label: "Order History" },
      { href: "/cart", label: "Cart" },
    ],
  },
  {
    title: "Resources",
    links: [
      { href: "/resources", label: "News & Guides" },
      { href: "/resources?type=case_study", label: "Case Studies" },
      { href: "/resources?type=guide", label: "Selection Guides" },
    ],
  },
]

export function Footer() {
  return (
    <footer className="mt-24 border-t border-[var(--color-line)] bg-[var(--color-surface-alt)]">
      <div className="shell grid gap-12 py-16 md:grid-cols-4">
        <div>
          <div className="text-lg font-bold tracking-tight">
            ACME<span className="text-[var(--color-ink-muted)]"> Industrial</span>
          </div>
          <p className="mt-3 max-w-xs text-sm leading-relaxed text-[var(--color-ink-muted)]">
            Authorized distributor of Selec Controls industrial automation
            products. Stocked in India, shipped pan-India with GST invoicing.
          </p>
        </div>
        {COLUMNS.map((col) => (
          <div key={col.title}>
            <h3 className="text-sm font-semibold">{col.title}</h3>
            <ul className="mt-4 space-y-2 text-sm text-[var(--color-ink-muted)]">
              {col.links.map((link) => (
                <li key={link.href}>
                  <Link href={link.href} className="hover:text-[var(--color-ink)]">
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </div>
        ))}
      </div>
      <div className="border-t border-[var(--color-line)]">
        <div className="shell flex h-12 items-center justify-between text-xs text-[var(--color-ink-muted)]">
          <span>© {new Date().getFullYear()} ACME Industrial. All rights reserved.</span>
          <span>All prices inclusive of GST</span>
        </div>
      </div>
    </footer>
  )
}
