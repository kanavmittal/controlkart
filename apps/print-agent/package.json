{
  "name": "@controlkart/print-agent",
  "version": "1.0.0",
  "private": true,
  "description": "ControlKart warehouse label-print daemon (Raspberry Pi). Polls the Medusa backend for print jobs, downloads Shiprocket label files, and sends them to a Zebra USB printer or CUPS.",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc && node dist/index.js",
    "build": "tsc",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.13",
    "@types/node": "^20.12.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20"
  }
}
