# Test-runner DB credentials (@medusajs/test-utils creates a throwaway DB per suite).
# Local Homebrew Postgres has no "postgres" role — use the machine user.
DB_USERNAME=kanavmittal
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
