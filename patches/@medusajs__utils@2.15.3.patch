diff --git a/dist/dal/index.d.ts b/dist/dal/index.d.ts
index c44504de209c8ea6985862665f853e4c6a1927a5..e7f016ecad1a71f25a95502d16c9bd36327bfb7d 100644
--- a/dist/dal/index.d.ts
+++ b/dist/dal/index.d.ts
@@ -10,3 +10,4 @@ export * from "./mikro-orm/mikro-orm-soft-deletable-filter";
 export * from "./mikro-orm/utils";
 export * from "./utils";
 //# sourceMappingURL=index.d.ts.map
+export * from "./mikro-orm/prune-find-options-against-metadata";
diff --git a/dist/dal/index.js b/dist/dal/index.js
index e1d41574538dd84a31da700b2a936f34348cbbbc..9c1623cc1ea25fa40d89d2d71ad739b71161c1de 100644
--- a/dist/dal/index.js
+++ b/dist/dal/index.js
@@ -26,3 +26,4 @@ __exportStar(require("./mikro-orm/mikro-orm-soft-deletable-filter"), exports);
 __exportStar(require("./mikro-orm/utils"), exports);
 __exportStar(require("./utils"), exports);
 //# sourceMappingURL=index.js.map
+__exportStar(require("./mikro-orm/prune-find-options-against-metadata"), exports);
diff --git a/dist/dal/mikro-orm/mikro-orm-repository.js b/dist/dal/mikro-orm/mikro-orm-repository.js
index cba39c550d17e36f8f3c9563824c678a8ba08673..0320cb6503b1304710910f0f92a110dfd83bdcc4 100644
--- a/dist/dal/mikro-orm/mikro-orm-repository.js
+++ b/dist/dal/mikro-orm/mikro-orm-repository.js
@@ -264,6 +264,7 @@ function mikroOrmBaseRepositoryFactory(entity) {
                     });
                 }
             }
+            require("./prune-find-options-against-metadata").pruneFindOptionsAgainstMetadata(manager.getDriver().getMetadata().get(this.entity.name), findOptions_.options);
             MikroOrmBaseRepository.compensateRelationFieldsSelectionFromLoadStrategy({
                 findOptions: findOptions_,
             });
@@ -281,6 +282,7 @@ function mikroOrmBaseRepositoryFactory(entity) {
                     });
                 }
             }
+            require("./prune-find-options-against-metadata").pruneFindOptionsAgainstMetadata(manager.getDriver().getMetadata().get(this.entity.name), findOptions_.options);
             MikroOrmBaseRepository.compensateRelationFieldsSelectionFromLoadStrategy({
                 findOptions: findOptions_,
             });
diff --git a/dist/dal/mikro-orm/prune-find-options-against-metadata.d.ts b/dist/dal/mikro-orm/prune-find-options-against-metadata.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a13d82adf4556275ad18edb64d3e3f8de596687b
--- /dev/null
+++ b/dist/dal/mikro-orm/prune-find-options-against-metadata.d.ts
@@ -0,0 +1,2 @@
+import type { EntityMetadata } from "@medusajs/deps/mikro-orm/core";
+export declare function pruneFindOptionsAgainstMetadata(meta: EntityMetadata<any> | undefined | null, options: { fields?: unknown; populate?: unknown }, logger?: {debug?: (message: string) => void}): { droppedFields: string[]; droppedPopulate: string[] };
diff --git a/dist/dal/mikro-orm/prune-find-options-against-metadata.js b/dist/dal/mikro-orm/prune-find-options-against-metadata.js
new file mode 100644
index 0000000000000000000000000000000000000000..723b7e12806cb813c08dfa76f7b6800672fdec76
--- /dev/null
+++ b/dist/dal/mikro-orm/prune-find-options-against-metadata.js
@@ -0,0 +1,88 @@
+"use strict";
+Object.defineProperty(exports, "__esModule", { value: true });
+exports.pruneFindOptionsAgainstMetadata = pruneFindOptionsAgainstMetadata;
+const stripStrategy = (segment) => segment.split(":")[0];
+const isFieldPathResolvable = (rootMeta, path) => {
+    if (path === "*") {
+        return true;
+    }
+    const segments = path.split(".");
+    let currentMeta = rootMeta;
+    for (let i = 0; i < segments.length; i++) {
+        const segment = stripStrategy(segments[i]);
+        const isLast = i === segments.length - 1;
+        if (segment === "*") {
+            return isLast && !!currentMeta;
+        }
+        if (!currentMeta) {
+            return false;
+        }
+        const prop = currentMeta.properties?.[segment];
+        if (!prop) {
+            return false;
+        }
+        if (!isLast) {
+            if (!prop.targetMeta) {
+                return false;
+            }
+            currentMeta = prop.targetMeta;
+        }
+    }
+    return true;
+};
+/**
+ * Drop entries from `options.fields` and `options.populate` whose dotted
+ * paths can't be resolved against the local entity's MikroORM metadata.
+ *
+ * Why: MikroORM 6.6.14 derives populate hints from nested `fields`
+ * entries and merges them into `options.populate`. If any derived hint
+ * references a non-relation or a missing property, MikroORM rejects it
+ * at populate-resolution time. Pruning at the DAL boundary keeps that
+ * branch harmless and matches the silent no-op behavior of 6.6.12 while
+ * still surfacing dropped paths to debug logs.
+ */
+function pruneFindOptionsAgainstMetadata(meta, options, logger) {
+    const droppedFields = [];
+    const droppedPopulate = [];
+    if (!meta || !options) {
+        return { droppedFields, droppedPopulate };
+    }
+    if (Array.isArray(options.fields)) {
+        options.fields = options.fields.filter((f) => {
+            if (typeof f !== "string") {
+                return true;
+            }
+            if (isFieldPathResolvable(meta, f)) {
+                return true;
+            }
+            droppedFields.push(f);
+            return false;
+        });
+    }
+    if (Array.isArray(options.populate)) {
+        options.populate = options.populate.filter((p) => {
+            if (typeof p !== "string") {
+                return true;
+            }
+            // Populate uses the same resolvability rule as fields: non-final
+            // segments must be real relations, the final segment can be a
+            // scalar. Medusa's buildQuery routinely sends paths like
+            // "payments.captures.amount" through populate from the `relations`
+            // config, where the trailing segment is a scalar projection.
+            if (isFieldPathResolvable(meta, p)) {
+                return true;
+            }
+            droppedPopulate.push(p);
+            return false;
+        });
+    }
+    if (logger?.debug) {
+        if (droppedFields.length) {
+            logger.debug(`Pruned non-resolvable fields against entity ${meta.className}: ${droppedFields.join(", ")}`);
+        }
+        if (droppedPopulate.length) {
+            logger.debug(`Pruned non-resolvable populate hints against entity ${meta.className}: ${droppedPopulate.join(", ")}`);
+        }
+    }
+    return { droppedFields, droppedPopulate };
+}
